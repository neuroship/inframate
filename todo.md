## inframate.sh
we are going to make a dramatic change in this project to make a open-source version of it which is more like a CLI tool installable from brew for example.
and that is going to be configurable from cli.

- remove the UI setup steps from git => this will only be running from the dir of terraform project
- add releavn cli command instaallable and the args 
- when we run it will run a server exposing a local web app on a port
- remove OIDC and user relted features
- make it ready to be packagable
- make it easy to publish to brew
- update docs 
- review
  
